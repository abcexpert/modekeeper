# One case-insensitive substring per line. Matched against wheel member paths.
# Keep this list focused on paid-core apply code that must never ship in public modekeeper wheels.
modekeeper/actuators/
